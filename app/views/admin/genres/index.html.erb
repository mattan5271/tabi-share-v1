<%= form_with model: @genre, url: admin_genres_path, local: true do |f| %>
<div class="row">
	<div class="col-xs-3">
		<%= f.text_field :name, placeholder: "ジャンル名", class: "form-control" %>
	</div>
	<div>
		<div class="col-xs-2">
			<%= f.submit "新規登録", class: "btn btn-success" %>
		</div>
	</div>
</div>


<% end %>

<table class="table table-bordered table-hover genre__table">
	<thead>
		<tr class="active">
			<th>ID</th>
			<th>ジャンル名</th>
			<th></th>
			<th></th>
		</tr>
	</thead>

	<tbody>
		<% @genres.each do |genre| %>
		<tr>
			<td><%= genre.id %></td>
			<td><%= genre.name %></td>
			<td><%= link_to "編集", edit_admin_genre_path(genre), class: "btn btn-primary btn-block" %></td>
			<td>
				<%= link_to "削除", admin_genre_path(genre), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger btn-block" %>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>