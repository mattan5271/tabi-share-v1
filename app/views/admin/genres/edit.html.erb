<div class="row">
  <div class="col-xs-3"></div>
  <div class="col-xs-6">
    <%= form_with model: @genre, url: admin_genres_path, local: true do |f| %>
    <%= f.text_field :name, placeholder: "ジャンル名", class: "form-control genre__form" %>
    <%= f.submit "保存", class: "btn btn-primary btn-block genre__button" %>
    <% end %>
  </div>
  <div class="col-xs-3"></div>
</div>