<h1>コメント編集画面</h1>

<%= form_with model: @comment, url: user_comment_path(@comment), local: true do |f| %>

<%= f.label :title, "タイトル" %><br>
<%= f.text_field :title %><br>

<%= f.label :body, "本文" %><br>
<%= f.text_area :body %><br>

<%= f.submit "保存" %>

<% end %>
